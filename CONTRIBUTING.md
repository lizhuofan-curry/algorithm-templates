# 仓库整理规范

这份规范用于让新增模板保持统一、易找、可运行。

## 新增内容清单

1. 把 C++ 文件放入对应知识模块；没有合适目录时再创建新目录。
2. 文件名应说明算法或题型，避免使用 `1.cpp`、`test.cpp` 等无法辨认的名称。
3. 模板使用 C++17，并保留可以独立编译的 `main` 函数。
4. 在代码开头说明适用场景、输入输出和数据范围。
5. 在配套笔记中记录核心思路、复杂度、边界条件和易错点。
6. 新知识点加入 [模板总目录](CATALOG.md)。
7. 提交前运行仓库检查。

## 命名约定

- 中文知识点沿用仓库现有中文目录，常见算法名保留标准英文缩写，如 `BFS`、`KMP`、`SPFA`。
- 同一算法存在多种实现时，用“朴素版”“优化版”等后缀明确区分。
- 不使用 Windows 不支持的文件名，例如以空格或英文句点结尾的名称，以及 `CON`、`NUL` 等保留名。
- 图片统一放在对应笔记旁的 `图片/` 目录中，并使用能说明内容的名称。

## 提交前检查

```bash
python scripts/check_repository.py --compile-cpp
```

检查全部通过后再提交。如果某份代码是思路草稿、暂时无法独立编译，应先保留在笔记代码块中，不要作为正式 `.cpp` 模板提交。
